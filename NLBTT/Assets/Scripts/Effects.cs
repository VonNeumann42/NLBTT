using UnityEngine;
using static Card;
using static Cardtypes;
//using static CardGenerator;
using static Player;
using static Events;
using static Terrains;
using static Items;

public class Effects : MonoBehaviour
{
   /* public void Effect (CardTypes cardTypes)
    {
        if  (cardTypes == CardTypes.Event)
        {
            Events.EventRule(Card:card);
        }
        else if (cardTypes == CardTypes.Terrain)
        {
            Terrains.TerrainRule( Card);
        }
        else if (cardTypes == CardTypes.Item)
        {
            Items.ItemRule();
        }
     }*/
}
